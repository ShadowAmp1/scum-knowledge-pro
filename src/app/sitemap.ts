import type { MetadataRoute } from "next"; import { bunkers } from "@/data/bunkers"; import { getContentData } from "@/lib/content";
const baseUrl=process.env.NEXT_PUBLIC_SITE_URL||"https://scumdbpro.duckdns.org";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const data=await getContentData(); const staticRoutes=["","/search","/weapons","/weapons/compare","/weapons/attachments","/loot","/missions","/bunkers","/map","/bases","/vehicles","/preparation","/guides","/favorites","/profile","/trackers","/calculators","/discord","/admin","/pro-roadmap"]; return [...staticRoutes.map(route=>({url:`${baseUrl}${route}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:route===""?1:.8})),...data.weapons.map(i=>({url:`${baseUrl}/weapons/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7})),...data.attachments.map(i=>({url:`${baseUrl}/weapons/attachments/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.6})),...data.loot.map(i=>({url:`${baseUrl}/loot/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.6})),...data.missions.map(i=>({url:`${baseUrl}/missions/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7})),...bunkers.map(i=>({url:`${baseUrl}/bunkers/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7})),...data.guides.map(i=>({url:`${baseUrl}/guides/${i.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.6}))]}
