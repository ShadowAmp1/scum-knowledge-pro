import { NextResponse } from "next/server";
import { getAdminSession } from "@/lib/adminSession";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function POST(request: Request) { if (!getAdminSession()) return NextResponse.json({ ok:false, message:"Нужен вход в админ-панель" }, { status:401 }); const webhook = process.env.DISCORD_WEBHOOK_URL; if (!webhook) return NextResponse.json({ ok:false, message:"DISCORD_WEBHOOK_URL не задан" }, { status:400 }); const body = await request.json().catch(()=>({})); const response = await fetch(webhook,{ method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ username:"SCUM DB PRO", embeds:[{ title: body.title || "SCUM DB PRO обновлён", description: body.message || "На сайте появились новые материалы.", url: body.url || process.env.NEXT_PUBLIC_SITE_URL || "https://scumdbpro.duckdns.org", color:15158332, timestamp:new Date().toISOString() }] }) }); if(!response.ok) return NextResponse.json({ ok:false, message:`Discord webhook вернул ${response.status}` }, { status:502 }); return NextResponse.json({ ok:true }); }
