"use client";
import { useEffect, useState } from "react"; import { Save, UserRound } from "lucide-react";
type Profile={nickname:string;playstyle:string;server:string;baseSector:string;notes:string}; const defaults:Profile={nickname:"",playstyle:"PvE / PvP",server:"",baseSector:"",notes:""}; const key="scum-db-pro-profile-v1";
export function ProfileDashboardClient(){const[profile,setProfile]=useState<Profile>(defaults);const[saved,setSaved]=useState(false);useEffect(()=>{try{const raw=localStorage.getItem(key);if(raw)setProfile({...defaults,...JSON.parse(raw)})}catch{}},[]);function update<K extends keyof Profile>(field:K,value:Profile[K]){setProfile(c=>({...c,[field]:value}));setSaved(false)}function save(){try{localStorage.setItem(key,JSON.stringify(profile));setSaved(true)}catch{}}return <section className="mx-auto max-w-5xl px-4 py-10"><div className="rounded-3xl border border-zinc-800 bg-zinc-950/80 p-6"><div className="flex items-center gap-3 text-red-300"><UserRound/><span className="text-sm font-black uppercase tracking-[0.25em]">Player profile</span></div><div className="mt-6 grid gap-4 md:grid-cols-2"><Field label="Ник / позывной" value={profile.nickname} onChange={v=>update("nickname",v)} placeholder="Shadow"/><Field label="Стиль игры" value={profile.playstyle} onChange={v=>update("playstyle",v)} placeholder="PvE / PvP / Duo"/><Field label="Сервер" value={profile.server} onChange={v=>update("server",v)} placeholder="Название сервера"/><Field label="Сектор базы" value={profile.baseSector} onChange={v=>update("baseSector",v)} placeholder="Например B0"/></div><label className="mt-4 block"><span className="mb-2 block text-xs font-black uppercase tracking-[0.2em] text-zinc-500">Заметки</span><textarea value={profile.notes} onChange={e=>update("notes",e.target.value)} rows={6} className="w-full rounded-2xl border border-zinc-800 bg-black p-4 text-sm text-white outline-none focus:border-red-500"/></label><button onClick={save} className="mt-5 inline-flex items-center gap-2 rounded-2xl bg-red-600 px-5 py-3 text-sm font-black text-white hover:bg-red-500"><Save size={16}/>Сохранить профиль</button>{saved?<span className="ml-3 text-sm font-bold text-emerald-300">Сохранено локально</span>:null}</div></section>}
function Field({label,value,onChange,placeholder}:{label:string;value:string;onChange:(value:string)=>void;placeholder:string}){return <label className="block"><span className="mb-2 block text-xs font-black uppercase tracking-[0.2em] text-zinc-500">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className="h-12 w-full rounded-2xl border border-zinc-800 bg-black px-4 text-sm text-white outline-none focus:border-red-500"/></label>}
