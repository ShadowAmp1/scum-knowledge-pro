"use client";
import { useState } from "react"; import { Send } from "lucide-react";
export function DiscordAnnounceClient(){const[title,setTitle]=useState("SCUM DB PRO обновлён"),[message,setMessage]=useState("На сайте добавлены новые материалы, квесты, карта и трекеры."),[status,setStatus]=useState("");async function send(){setStatus("Отправка...");const res=await fetch("/api/discord/announce",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title,message,url:window.location.origin})});const json=await res.json().catch(()=>({}));setStatus(json.ok?"Отправлено в Discord":json.message??"Ошибка отправки")}return <section className="mx-auto max-w-4xl px-4 py-10"><div className="rounded-3xl border border-zinc-800 bg-zinc-950/80 p-6"><label className="block"><span className="mb-2 block text-xs font-black uppercase tracking-[0.2em] text-zinc-500">Заголовок</span><input value={title} onChange={e=>setTitle(e.target.value)} className="h-12 w-full rounded-2xl border border-zinc-800 bg-black px-4 text-sm text-white"/></label><label className="mt-4 block"><span className="mb-2 block text-xs font-black uppercase tracking-[0.2em] text-zinc-500">Сообщение</span><textarea value={message} onChange={e=>setMessage(e.target.value)} rows={6} className="w-full rounded-2xl border border-zinc-800 bg-black p-4 text-sm text-white"/></label><button onClick={send} className="mt-5 inline-flex items-center gap-2 rounded-2xl bg-red-600 px-5 py-3 text-sm font-black text-white"><Send size={16}/>Отправить webhook</button>{status?<div className="mt-4 rounded-2xl border border-zinc-800 bg-black p-4 text-sm text-zinc-300">{status}</div>:null}</div></section>}
