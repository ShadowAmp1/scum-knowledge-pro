# SCUM Knowledge PRO v4

Готовый фан-сайт-база знаний по SCUM: оружие, бункеры, лут, подготовка к рейдам и интерактивная карта.

## Что нового в v4

- интерактивная карта `/map`;
- поиск по меткам;
- фильтры по бункерам, луту, транспорту, торговцам, базам, опасным зонам, городам и военным зонам;
- карточка выбранной точки;
- риск, tier лута и рекомендуемый набор;
- быстрые переходы с карты в связанные разделы;
- данные карты редактируются в `src/data/mapMarkers.ts`.

## Стек

- Next.js 14
- TypeScript
- Tailwind CSS
- Lucide React
- Render-ready конфигурация

## Разделы

- Главная страница
- Оружие: поиск, фильтры, tier-list, отдельные страницы оружия
- Бункеры: поиск, фильтры, риск, маршруты, подготовка, враги, лут
- Лут: поиск, категории, редкость, приоритет, места фарма
- Подготовка: raid-kit чек-листы
- Интерактивная карта: метки, поиск, фильтры и карточки точек
- Базы
- Транспорт
- Гайды
- PRO roadmap

## Запуск на ПК

```bash
npm install
npm run dev
```

Открыть:

```txt
http://localhost:3000
```

## Проверка сборки

```bash
npm run build
```

## Деплой на Render

Для Render уже есть файл `render.yaml`.

Настройки вручную, если нужны:

```txt
Build Command: npm install && npm run build
Start Command: npm run start
```

Environment Variable:

```txt
NODE_VERSION=20
```

или:

```txt
NODE_VERSION=22
```

## Как обновить GitHub

Скопируй файлы из архива в свой репозиторий, затем выполни:

```bash
git add .
git commit -m "Add SCUM DB PRO v4 interactive map"
git push
```

После push Render должен пересобрать сайт автоматически. Если не пересобрал — нажми в Render:

```txt
Manual Deploy → Deploy latest commit
```

## Где менять точки карты

Файл:

```txt
src/data/mapMarkers.ts
```

Поля координат:

```ts
x: 50,
y: 40,
```

`x` и `y` — проценты от размера карты, от 0 до 100.

## Следующий крупный этап

Следующий этап — v5: админ-панель, PostgreSQL, Prisma и возможность добавлять материалы через сайт без редактирования кода.

## v4.1: интерактивная карта по текущей карте SCUM

В версии v4.1 страница `/map` использует изображение `public/images/scum-current-map.png` как фон карты.

Метки лежат в файле:

```txt
src/data/mapMarkers.ts
```

Чтобы поправить положение метки, измени поля:

```ts
x: 50,
y: 23,
```

`x` — позиция слева направо в процентах, `y` — позиция сверху вниз в процентах.

Примеры:

```txt
центр карты: x: 50, y: 50
левый верхний угол: x: 0, y: 0
правый нижний угол: x: 100, y: 100
```


## v4.2 — карта бункеров

В версии v4.2 карта обновлена под текущий скрин:
- убрана сетка поверх карты;
- добавлены все 25 обычных бункеров;
- по умолчанию показываются только бункеры;
- safe zone / торговцы включаются отдельным фильтром;
- координаты точек можно править в `src/data/mapMarkers.ts`.
